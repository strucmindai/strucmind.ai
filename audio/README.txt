Drop your ambient loop here as `ambient.mp3`.

The audio toggle in the Navbar plays this file on a loop at 35% volume.
It is paused by default (browser autoplay policies require a user gesture).

If the file is missing, the toggle UI still renders and the toggle()
attempt is swallowed silently — nothing breaks.
